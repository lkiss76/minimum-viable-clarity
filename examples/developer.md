# MVC Example — Software Developer

## Intent
Generate a deterministic implementation guide.

## Context
Developer integrating a new API; needs precise technical steps.

## Criteria
- Must include code examples  
- List dependencies  
- Steps must be sequential  
- No business context  

## Exemplar
1. Install dependency: `npm install sdk-x`  
2. Add env variable: `API_KEY`  
3. Initialize client: `const client = new SDK(process.env.API_KEY)`  
